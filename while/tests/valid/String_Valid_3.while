string repl(char old, char n, string str) {
    int i = 0;
    while(i < |str|) {
        if(str[i] == old) {
            str[i] = n;
        }
        i = i + 1;
    }
    return str;
}

void main() {
    string s = "Hello World";
    s = repl('l','1',s);
    print s;
}
